{
  "name": "example-workerd",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node run.js",
    "build": "pnpm run build:client && pnpm run build:server",
    "build:client": "vite build --mode=client",
    "build:server": "vite build",
    "preview": "vite preview",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230821.0",
    "vite-node-workerd": "workspace:*",
    "typescript": "^5.0.2",
    "vite": "^4.4.9",
    "vitest": "^0.34.3"
  }
}
