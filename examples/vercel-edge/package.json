{
  "name": "example-vercel-edge",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite-env",
    "build": "pnpm run build:client && pnpm run build:server",
    "build:client": "vite build --mode=client",
    "build:server": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@vite-env/vercel-edge": "workspace:*",
    "@vite-env/core": "workspace:*",
    "@vite-env/standalone": "workspace:*",
    "typescript": "^5.0.2",
    "vite": "^4.4.9"
  }
}
