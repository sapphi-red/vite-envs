{
  "name": "example-vercel-edge",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "pnpm run build:client && pnpm run build:server",
    "build:client": "vite build --mode=client",
    "build:server": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^20.11.19",
    "@vite-runtime/vercel-edge": "workspace:*",
    "example-framework": "workspace:*",
    "typescript": "^5.3.3",
    "vite": "^5.1.3"
  }
}
